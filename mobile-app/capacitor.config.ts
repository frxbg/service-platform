import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.serviceplatform.technician',
  appName: 'Service Technician',
  webDir: 'dist',
};

export default config;
